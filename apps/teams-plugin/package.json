{
  "name": "@onboarding/teams-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && vite build",
    "dev": "vite --host 0.0.0.0",
    "lint": "eslint .",
    "test": "tsx --test src/*.test.tsx",
    "preview": "vite preview"
  },
  "dependencies": {
    "@onboarding/shared": "file:../../packages/shared"
  }
}
