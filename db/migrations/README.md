# Historical Migration Archive

These SQL files document the migration workflow used before Prisma adoption. They are immutable
history and are not an active deployment source.

Use `prisma/migrations` and the root `db:migrate:*` commands for all schema changes and deployments.
For an existing database that already ran these files, follow
`docs/prisma-migration-adoption.md` before the first Prisma deployment.
